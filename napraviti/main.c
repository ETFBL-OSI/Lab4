#include <stdio.h>
#include <math.h>
double stepenovanje(double baza, double eksponent){
    return pow(baza,eksponent);
}

int main()
{
    /*
    double a = 2;
    double b = 3;
    double c = stepenovanje(a,b);
    printf("%lf", c);
    */
    return 0;
}
